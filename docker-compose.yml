version: '3.8'

services:
  esms-api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: esms-optimizer
    ports:
      - "8000:8000"
    environment:
      - LOG_LEVEL=INFO
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
